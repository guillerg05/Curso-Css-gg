
const pendientes = ['Tarea', 'Comer', 'Proyecto', 'Aprender JavasCript'];

//forEach
pendientes.forEach(function(pendiente){
  console.log(`${pendiente}`);
});

console.log(pendientes);

/*let n = parseInt (prompt("Digita un número"));
let i = 1; factorial = 1;
if (n > 0) {
    //Factorial
while (i <= n) {
    factorial = factorial * i;
    i++;
}
alert ("El factorial de "+n+" es: "+factorial);
} else {
    alert ("Debes digitar el número entero positivo")
}
*/




/*
for (var i=1; i<=20; i++ ){
    if (i==5){break;}
    document.write(i + "<br>");
}
*/


 /* Machetes
 <= menor igual que
 >= mayor igual que
 == igualdad
 != diferente
 && "y" las dos se cumplen
 || "o" puede que una de las opciones no se cumpla

 !() niega lo que esta adentro del paerntesis
 si lo que esta adentro NO ES VERDADERO te pone verdadero. Ej
 var uno = 19; var dos =19
 Document.write(!(uno==20)). en el navegador responde true
 a!="" que a no sea una cadena vacia

 condicion ? TRUE : false;
 */
